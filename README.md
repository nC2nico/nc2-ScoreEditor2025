## アプリケーションの起動方法

1. サーバー側（serverフォルダ）:
    ```bash
    cd server
    npm install
    npm run dev
    ```

2. クライアント側（clientフォルダ）:
    ```bash
    cd client
    npm install
    npm run dev
    ```

以上でアプリケーションが起動します。